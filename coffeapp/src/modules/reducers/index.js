import React from 'react'

export default function coffeeOrderReducer(state = [],action) {
   switch (action.type) {
       case value:
           
           break;
   
       default:
           break;
   }
}
